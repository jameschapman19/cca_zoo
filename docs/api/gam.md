# cca_zoo.gam

Generalized-additive-model (GAM) nonlinear CCA methods, fit by a single joint trust-region
Newton-CG solve (P-IRLS-style). Built entirely on scikit-learn's own `SplineTransformer` and
`scipy.optimize` — no optional dependency required.

---

::: cca_zoo.gam.GAMCCA
